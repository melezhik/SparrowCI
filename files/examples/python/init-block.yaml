tasks:
  -
    name: main
    language: Python
    default: true
    init: |
      set_stdout("some initialization code")
    code: |
      print("main code")
    check: |
      begin:
        some initialization code
        main code
      end:
